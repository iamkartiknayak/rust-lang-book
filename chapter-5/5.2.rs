// 5.2 => Structs - Tuple Structs

fn main() {
    struct Color(i32, i32, i32);
    struct Point(i32, i32, i32);
}

/*
Notes:
- If a fn expects a tuple of type Color only Color(i32, i32, i32) is valid
*/
