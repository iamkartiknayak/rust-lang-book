fn main() {
    println!("Hello, world!");
}

/*
Notes:
- A package must have at-least 1 crate, here for `main.rs` which is crate root
- A package can have 0 or 1 library crates
- A package can have multiple binary crates `bin/` is an eg
*/
